Customized this file with custom class names. Also removed all gritter-class names, so everything goes under the name 'micnotification'.

Also changed 'image' into icon class, so we have can use icon-sprites.
